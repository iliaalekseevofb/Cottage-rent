import React from 'react';
import { useParams } from 'react-router-dom';

const roomInfo = [
  
]

const RoomDetails = () => {
  const {id} = useParams();

  return (
    <div>RoomDetails</div>
  )
}

export default RoomDetails